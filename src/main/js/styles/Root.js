const getScreenHeaderStyles = ({ colors }) => ({
  headerStyle: {
    backgroundColor: colors.background
  },
  headerTintColor: colors.text,
  headerTitleStyle: {
    fontFamily: 'serif',
    fontWeight: '400'
  }
});

export default getScreenHeaderStyles;
